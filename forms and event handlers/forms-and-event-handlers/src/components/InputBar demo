import React from 'react';

class InputBar extends React.Component {
    onInputChange (event){
        console.log(event.target.value);
    }

    // <input type="text" onChange={this.onInputChange}/> - a reference to the method
    // so that we can call this function at some point of time
    render() {
        return (
            <div className="ui segment">
                <form className="ui form">
                    <div className="field">
                        <label>My list</label>
                        <input type="text" onChange={this.onInputChange}/>
                    </div>
                </form>

            </div>
        );
    }
}

export default InputBar;

// onChange
// onClick
// onSubmit